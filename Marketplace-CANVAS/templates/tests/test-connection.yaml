apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Marketplace-CANVAS.fullname" . }}-test-connection"
  labels:
    {{- include "Marketplace-CANVAS.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Marketplace-CANVAS.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
